<%= form_for @admin, :url => password_reset_path(params[:id]) do |f| %>
  <% if @admin.errors.any? %>
    <div class="error_messages">
      <ul>
        <% for message in @admin.errors.full_messages %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <%= f.label "Contraseña" %>
    <%= f.password_field :password %>
  </div>
  <div class="field">
    <%= f.label "Confirmación de contraseña" %>
    <%= f.password_field :password_confirmation %>
  </div>
  <div class="actions"><%= f.submit "Editar Contraseña" %></div>
<% end %>