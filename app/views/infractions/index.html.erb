<h1>Listing infractions</h1>

<table>
  <thead>
    <tr>
      <th>Description</th>
      <th>Latitude</th>
      <th>Longitude</th>
      <th>Address</th>
      <th>License code</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @infractions.each do |infraction| %>
      <tr>
        <td><%= infraction.description %></td>
        <td><%= infraction.latitude %></td>
        <td><%= infraction.longitude %></td>
        <td><%= infraction.address %></td>
        <td><%= infraction.license_code %></td>
        <td><%= link_to 'Show', infraction %></td>
        <td><%= link_to 'Edit', edit_infraction_path(infraction) %></td>
        <td><%= link_to 'Destroy', infraction, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Infraction', new_infraction_path %>
